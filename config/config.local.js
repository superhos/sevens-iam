'use strict';

module.exports = appInfo => {
  const config = exports = {};

  config.systemInfo = {
    name: 'Sevens IAM',
    subtitle: 'Sevens Identity And Access Management Service'
  }

  return config;
};